{% extends 'base.html.twig' %}

{% block title %}Hello ProductsController!{% endblock %}

{% block body %}

    <div class="row mt-3">
        {% for product in products %}
            {{ include('dashboard/_product_card.html.twig') }}
        {% endfor %}
    </div>
    <a href="{{ path('app_product_new') }}">Ajouter</a>
{% endblock %}
